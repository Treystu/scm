<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security configuration for SCMessenger.

  Production: cleartext traffic is disabled by default.
  Loopback addresses used during local development (emulator host and
  adb reverse) are exempted so internal tooling keeps working.

  See: https://developer.android.com/training/articles/security-config
-->
<network-security-config>
    <!-- Block cleartext for all domains by default. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
      Allow cleartext for local loopback addresses only. This is the
      Android emulator host alias (10.0.2.2) and the local loopback
      (127.0.0.1) used by adb reverse / on-device proxies during
      development. Production traffic is unaffected because it goes
      over the public mesh-relay servers via TLS / libp2p noise.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
