# Android builds share the root cargo config for linker settings.
# Root `.cargo/config.toml` defines the explicit MSVC linker path.
# This file is intentionally minimal; only Android-specific overrides belong here.

[build]
# Disable incremental compilation to prevent rlib metadata errors on Windows.
# This mirrors the root `.cargo/config.toml` setting.
incremental = false
